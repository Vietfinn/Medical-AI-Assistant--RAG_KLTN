# --- BACKEND FRAMEWORK ---
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.3
pydantic-settings>=2.1.0
python-dotenv>=1.0.0
python-multipart>=0.0.6
aiofiles>=23.2.1

# --- VECTOR DATABASE & STORAGES ---
# Cần >= 1.10.0 để hỗ trợ Prefetch + Fusion (RRF) cho Hybrid Search native
qdrant-client>=1.10.0
# MongoDB Async Driver cho History Chat
motor>=3.3.2
pymongo>=4.6.1

# --- SPARSE EMBEDDING (BM25 → Qdrant Native) ---
# fastembed mã hoá văn bản thành sparse vectors cho Qdrant sparse_vectors_config
fastembed>=0.2.6

# --- EMBEDDINGS & ML ---
sentence-transformers>=2.6.0
torch
transformers>=4.38.0
datasets>=2.16.0

# --- COHERE RERANKER API ---
# Rerank-v4.0-pro thay thế local CrossEncoder, giảm latency từ ~24s xuống <1s
cohere>=5.0.0

# --- LARGE LANGUAGE MODEL ---
# Gemini 2.5 Flash - Clinical RAG Agent
google-generativeai>=0.5.0

# --- GROQ (Llama 3 Runtime) ---
# Llama 3.3 70B - Triage Agent & Safety Guard Agent
groq>=0.9.0

# --- AUTHENTICATION (Clerk JWT) ---
PyJWT>=2.8.0
cryptography>=42.0.0

# --- EMAIL (Gmail SMTP) ---
# Built-in smtplib used, no external packages required

# --- DATA PROCESSING & NLP ---
numpy
pandas>=2.1.4
underthesea>=6.7.0
# rank-bm25 đã được thay thế bằng Qdrant native sparse vectors (fastembed)